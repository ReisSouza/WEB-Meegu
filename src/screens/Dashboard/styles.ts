import { styled } from '@ionext-ui/react'

export const DashboardContainer = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  gap: '$6',
})

export const DashboardFlexRow = styled('div', {
  display: 'flex',
})
export const DashboardFlexCol = styled('div', {
  display: 'flex',
  flexDirection: 'column',
})
